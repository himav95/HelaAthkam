import {Nav} from 'react-bootstrap';


// Page navigation and active page (pageNavBar) in header.jsx
function pageNavigation ({activeKey, handleNavigation}) {
    return ( )

}

export default pageNavigation